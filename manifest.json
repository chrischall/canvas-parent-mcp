{
  "dxt_version": "0.1",
  "name": "canvas-parent-mcp",
  "version": "0.1.3",
  "description": "Canvas LMS (Instructure) MCP — student/observer read access with optional file download",
  "author": {
    "name": "Claude Code (AI)"
  },
  "server": {
    "type": "node",
    "entry_point": "dist/bundle.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/bundle.js"
      ],
      "env": {
        "CANVAS_BASE_URL": "${user_config.canvas_base_url}",
        "CANVAS_TOKEN": "${user_config.canvas_token}",
        "CANVAS_CLIENT_ID": "${user_config.canvas_client_id}",
        "CANVAS_CLIENT_SECRET": "${user_config.canvas_client_secret}",
        "CANVAS_REFRESH_TOKEN": "${user_config.canvas_refresh_token}",
        "CANVAS_NAME": "${user_config.canvas_name}"
      }
    }
  },
  "user_config": {
    "canvas_base_url": {
      "type": "string",
      "title": "Canvas base URL (https://...)",
      "description": "Canvas instance base URL, e.g. https://cms.instructure.com",
      "required": true
    },
    "canvas_token": {
      "type": "string",
      "title": "Personal access token (recommended)",
      "description": "Canvas personal access token. Generate via Account → Settings → '+ New Access Token'. Set this OR all three of canvas_client_id + canvas_client_secret + canvas_refresh_token.",
      "required": false,
      "sensitive": true
    },
    "canvas_client_id": {
      "type": "string",
      "title": "OAuth client ID (optional)",
      "description": "Canvas Developer Key client ID. Required only if not using a personal access token.",
      "required": false
    },
    "canvas_client_secret": {
      "type": "string",
      "title": "OAuth client secret (optional)",
      "description": "Canvas Developer Key client secret. Required only if not using a personal access token.",
      "required": false,
      "sensitive": true
    },
    "canvas_refresh_token": {
      "type": "string",
      "title": "OAuth refresh token (optional)",
      "description": "Canvas refresh token from a one-time auth-code flow. Required only if not using a personal access token.",
      "required": false,
      "sensitive": true
    },
    "canvas_name": {
      "type": "string",
      "title": "Display name (optional)",
      "description": "Friendly name for the Canvas instance (e.g. \"CMS\"). Defaults to the host portion of the base URL.",
      "required": false
    }
  }
}
